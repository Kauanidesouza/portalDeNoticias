## Instalar Angular 
`npm install -g @angular/cli`
##criar o projeto do site
## criar o projeto do site 
npx ng new portal 
## Entrar na pasta do projeto 
`cd potal `
## Inicializando o servidor angular(modo dev)
npx ng serve
## Cria um novo componente de cabeçalho
npx ng g c header
## Cria um novo componente de rodapé
npx ng g c footer
